'use strict';

/*
describe('Filter: entry', function () {

  // load the filter's module
  beforeEach(module('portfolio'));

  // initialize a new instance of the filter before each test
  var entry;
  beforeEach(inject(function ($filter) {
    entry = $filter('entry');
  }));

  it('should return the input prefixed with "entry filter:"', function () {
    var text = 'angularjs';
    expect(entry(text)).toBe('entry filter: ' + text);
  });

});
*/
