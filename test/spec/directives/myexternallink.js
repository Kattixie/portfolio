'use strict';

/*
describe('Directive: myExternalLink', function () {

  // load the directive's module
  beforeEach(module('portfolio'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<my-external-link></my-external-link>');
    element = $compile(element)(scope);
    expect(element.text()).toBe('this is the myExternalLink directive');
  }));
});
*/
